import React from 'react';

function Buttons() {
    return(
        <div class="buttons">
            <button>Tweet</button>
            <button>New Quote</button>
        </div>
    )
}

export default Buttons;